The operation is fairly straight forward.
In matlab add all folders and subfolders to path. 
Then type start into matlab cmd line.
The only point of interest is you need to place the checkerboard you are using for callibration over the top of the coins when prompted after performing the 'dance' so that the translation can happen.  